
<section class="container">
  <h3 class="user-name">{{user.username}}</h3>
  <section class="container">
    {{#unless isUser}}
    {{#if ships}}
    <h1>UNFOLLOW</h1>
    {{/if}}
    {{#unless ships}}
    <h1>FOLLOW</h1>
    {{/unless}}
    {{/unless}}
  </section>
  <section class="flex-row justify-center">
    {{#each user.posts}}
    {{> indivpost-details}}
    {{/each}}
  </section>
</section> 