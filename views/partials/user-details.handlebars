<section class="mr-1 card" style="width:20rem;">
  <div class="card-body">
    <div class="card-title">
      <a href="/user/{{id}}">
        <h4 class="card-title"><img
            src="{{profile_image_filename}}"
            class="img-thumbnail border-0 rounded-circle"
            style="width:30%"
            alt="profile pic placeholder"
          />{{username}}</h4>
      </a>
    </div>
  </div>
</section>