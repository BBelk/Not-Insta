<section class="card">
  <div class="card-body">
    <a href="/user/{{followee_id}}">
      <h1 class="card-title">{{user.username}}{{followee_id}}</h1>
      {{!-- <img src="{{url}}" class="img-fluid img-thumbnail" alt="{{name}}"> --}}
    </a>
  </div>
</section>