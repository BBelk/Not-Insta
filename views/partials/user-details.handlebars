<section class="card">
  <div class="card-body">
    <a href="/user/{{id}}">
    <img src="{{profile_image_filename}}" class="img-thumbnail rounded-circle " style="width:30%"
            alt="profile pic placeholder">
      <h1 class="card-title">{{username}}</h1>
      {{!-- <img src="{{url}}" class="img-fluid img-thumbnail" alt="{{name}}"> --}}
    </a>
  </div>
</section>