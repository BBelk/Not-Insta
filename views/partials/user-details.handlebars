<section class="card">
  <div class="card-body">
    <a href="/user/{{id}}">
      <h1 class="card-title"><img
          src="{{profile_image_filename}}"
          class="img-thumbnail rounded-circle"
          style="width:30%"
          alt="profile pic placeholder"
        />
        {{username}}</h1>
    </a>
  </div>
</section>