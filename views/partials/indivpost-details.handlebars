<section class="card">
  <a href="/post/{{id}}">
    <header>{{user.name}}</header>
    <div class="card-body">
      <img src="{{image_filename}}" alt="{{url}}">
      <p>{{body_text}}</p>
    </div>
  </a>
</section>